<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @field.title %>
</p>

<p>
  <strong>Size:</strong>
  <%= @field.size %>
</p>

<p>
  <strong>Number:</strong>
  <%= @field.number %>
</p>

<p>
  <strong>Description:</strong>
  <%= @field.description %>
</p>

<%= link_to 'Edit', edit_field_path(@field) %> |
<%= link_to 'Back', fields_path %>

<% plantings = @field.select_table(@field.id) %>

   <% plantings.each do |plant| %>
       <%= plant.crop.title %><br>
      
   <% end %>
